 a live neural network that scores ad creatives using both image (ResNet-50) and text (DistilBERT) features.
 Uses Gradio interface to Hugging Face Spaces: upload an ad image + paste copy -> get an engagement score, visual heatmap, highlighted copy, and 3 headline improvements in real time
